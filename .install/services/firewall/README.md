# Firewall

The base installation establishes the deny-by-default recovery and service baseline. This service boundary is reserved for the LESta-owned firewall table. Tenant-visible arbitrary firewall rules are deferred and must not be added to bootstrap.

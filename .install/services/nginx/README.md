# nginx

First-release web server capability. nginx is the only supported web server; Apache and nginx-in-front-of-Apache are deferred. Runtime templates belong to the Go agent release. Bootstrap owns package installation and the dedicated include root only.
